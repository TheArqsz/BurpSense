name: BurpSense Testing

on:
  workflow_dispatch:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - main

jobs:
  bridge-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./burp-bridge
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build and Run Unit Tests
        run: mvn clean test

      - name: Run Integration Tests
        run: mvn verify -DskipUnitTests

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: burp-bridge/target/site/jacoco/

  extension-tests:
    needs: bridge-tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./vscode-extension
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: 'npm'
          cache-dependency-path: './vscode-extension/package-lock.json'

      - name: Install Dependencies
        run: |
          # npm ci

      - name: Placeholder for Run Extension Tests
        run: |
          echo "Running VS Code Extension tests..."
          # npm test
          # Note: On Linux, you usually need 'xvfb-run' for VS Code tests
          # example: xvfb-run -a npm test
        
      - name: Extension Linting
        run: |
          npm run lint